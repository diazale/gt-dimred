# Testing python script with input

dset = input('Specify PC dataset (UKBB/HRS/1000G): ')
num_pcs = input('Number of principal components: ')
num_iter = input('Number of iterations: ')
imp = input('Specify t-SNE implementation (SKL/MC)')
